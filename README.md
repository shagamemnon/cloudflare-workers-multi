# A `worker-template` for managing multiple Workers

## Usage
- Modify `wrangler.toml`
- Deploy Workers using the `--env` flag:
  
```sh
wrangler publish --env sub_app_1

wrangler publish --env sub_app_2

wrangler publish --env sub_app_3
```
