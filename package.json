{
  "private": true,
  "name": "edge-worker-app",
  "version": "1.0.0",
  "description": "A template for multiple Cloudflare Workers in 1 repository",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --write '**/*.{js,css,json,md}'"
  },
  "author": "Frank Taylor <franklin@cloudflare.com>",
  "license": "MIT",
  "devDependencies": {
    "prettier": "^1.18.2"
  }
}
